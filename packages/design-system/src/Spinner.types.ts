export interface SpinnerProps {
  label?: string;
  /** Ring diameter on web/desktop. */
  size?: number;
  /** Sit inline in a row (a toolbar, a list header) instead of centring in the pane. */
  inline?: boolean;
}
